/**
 * Expanding Background Extension
 *
 * Adds a scroll-driven background effect that expands from a small blurred circle
 * to fill the entire section with color.
 *
 * @package
 */

// Import styles
import './editor.scss';

// Import extension modules
import './attributes';
import './editor';

// Note: frontend.js is imported in src/frontend.js for frontend-only loading
