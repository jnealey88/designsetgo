/**
 * Query Slider Block - Frontend Script
 *
 * Re-uses the exact same DSGSlider initialization from the slider block.
 * The slider's view.js targets all `.dsgo-slider` elements, which includes
 * the query-slider output since render.php uses the same class names.
 */
import '../slider/view';
